# Release v0.1.0 - First Scaffold Slice

Date: 2026-04-26
Commit: 49f0ed4

## Gate Results

- ruff: PASS (`uv run --all-packages ruff check`)
- mypy: PASS (`uv run --all-packages mypy`)
- pytest: PASS (`uv run --all-packages pytest -q` -> `26 passed`)

## Docker Compose Boot

- PASS: `uv run --all-packages pytest -q tests/e2e/test_docker_compose.py` -> `2 passed`
- Verified services: postgres, redis, api, worker
- Verified API health endpoint: `/healthz`

## Interface Validation Summary

HTTP endpoints verified:
- POST /v1/snapshots/git
- POST /v1/queries
- GET /v1/answers/{answer_id}
- GET /v1/review-queue

CLI commands verified:
- ingest git
- query
- answer get
- review list

## Test Summary

- Full suite: `26 passed` in local docker-capable run.
- Contract freeze and parity checks are passing.
- Docs consistency and README quick-start verification checks are passing.

## Notes

This release artifact captures the first-scaffold quality and runtime checkpoints. Tag creation remains a separate explicit step.
